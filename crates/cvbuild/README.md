# cvbuild (internal)

The `cvbuild` binary reads a (compressed) PSI-MS OBO file and generates a compressed bundle of
preparsed entries from the controlled vocabulary. This is used to build the precompiled bundle
that is embedded in the `mzdata-param` crate.

This is unlikely to have any use externally.
